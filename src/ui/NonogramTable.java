package ui;

import javax.swing.*;
import java.awt.image.BufferedImage;

public class NonogramTable extends JComponent {

    public NonogramTable(BufferedImage image) {
        int width = image.getWidth();
        int height = image.getHeight();
    }
}
